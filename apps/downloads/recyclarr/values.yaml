namespace: downloads

secrets:
  enabled: true
  type: infisical
  infisical:
    hostAPI: https://eu.infisical.com
    projectSlug: vizima
    envSlug: prod
    secretsPath: "/downloads/recyclarr"
    auth:
      credentialsSecretName: infisical-universal-auth
      credentialsSecretNamespace: infisical-system
    managedSecret:
      creationPolicy: Owner

components:
  recyclarr:
    image:
      repository: ghcr.io/recyclarr/recyclarr
      tag: "8.6.0"
      pullPolicy: IfNotPresent

    service:
      enabled: false

    volumes:
      pvc:
        - name: config
          mountPath: /config
          size: 1Gi
          readOnly: false
          accessMode: ReadWriteOnce
          storageClassName: longhorn
          labels:
            recurring-job.longhorn.io/source: enabled
            recurring-job-group.longhorn.io/backup: enabled
      emptyDir:
        - name: tmp
          mountPath: /tmp

    resources:
      requests:
        memory: 32Mi
        cpu: 5m
      limits:
        memory: 128Mi

    env:
      - name: TZ
        value: "Europe/Prague"
      - name: CRON_SCHEDULE
        value: "@daily"
      - name: RECYCLARR_CREATE_CONFIG
        value: "false"
      - name: RADARR_API_KEY
        valueFrom:
          secretKeyRef:
            name: recyclarr
            key: radarr_api_key
      - name: SONARR_API_KEY
        valueFrom:
          secretKeyRef:
            name: recyclarr
            key: sonarr_api_key

    configMap:
      enabled: true
      mountPath: /config/recyclarr.yml
      subPath: recyclarr.yml
      files:
        recyclarr.yml: |
          # yaml-language-server: $schema=https://schemas.recyclarr.dev/v8/config-schema.json

          radarr:
            movies:
              base_url: http://radarr.downloads.svc.cluster.local:7878
              api_key: !env_var RADARR_API_KEY

              quality_definition:
                type: movie

              quality_profiles:
                - name: 1080p
                  trash_id: 0896c29d74de619df168d23b98104b22
                  reset_unmatched_scores:
                    enabled: true
                  # the guide gates on release group tiers, rejecting most of what the
                  # configured indexers carry
                  min_format_score: 0
                  upgrade:
                    allowed: true
                    until_quality: Bluray|WEB-1080p
                    # the guide aims higher than any single release can score, so it
                    # never stops searching
                    until_score: 1700
                  qualities:
                    - name: Bluray|WEB-1080p
                      qualities:
                        - Bluray-1080p
                        - WEBRip-1080p
                        - WEBDL-1080p

              media_naming:
                folder: jellyfin-tmdb
                movie:
                  rename: true
                  # file format is owned in the app. every guide preset carries mediainfo
                  # audio tokens, and those are re-read from disk on rescan, so stripping
                  # audio tracks would move filenames out from under their sidecars

          sonarr:
            series:
              base_url: http://sonarr.downloads.svc.cluster.local:8989
              api_key: !env_var SONARR_API_KEY

              quality_definition:
                type: series

              quality_profiles:
                - name: 1080p
                  trash_id: 72dae194fc92bf828f32cde7744e51a1  # web-1080p
                  reset_unmatched_scores:
                    enabled: true
                  upgrade:
                    allowed: true
                    until_quality: Bluray|WEB-1080p
                    until_score: 1700
                  # the guide profile allows WEB only, so bluray releases already on
                  # disk would be treated as wrong quality and replaced
                  qualities:
                    - name: Bluray|WEB-1080p
                      qualities:
                        - Bluray-1080p
                        - WEBRip-1080p
                        - WEBDL-1080p

              media_naming:
                series: jellyfin-tvdb
                season: default
                episodes:
                  rename: true
                  # file format is owned in the app, same reason as radarr

    replicaCount: 1

    securityContext:
      runAsUser: 10001
      runAsGroup: 10001
      fsGroup: 10001
      runAsNonRoot: true
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      capabilities:
        drop:
          - ALL

    initContainers:
      fixPermissions:
        enabled: false

    probes:
      liveness:
        enabled: false
      readiness:
        enabled: false
      startup:
        enabled: false

    metrics:
      enabled: false
